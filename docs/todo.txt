- [x] integrate yank with os clipboard
- [x] make `gd` command work globally within a repo, i.e. with several files.
- [x] discovered that changes after `03afa45`, resulted in degregated speed in work M4 device.
	- thing only applies to repos like market | batch. With smaller repos, speed is not exploited.
	- first symptom: when opening a certain file, `nvim main.py` and try doing `hjkl` the neovim doesn't respond for 3~30 secs.
	- second symptom: after viewing or editing a file and trying to save quite or just quit, `:wq or :q`, neovim doesn't respond for a noticeable amount of time.
	- lastly, when running `nvim`, i get this messages down below:
	```bash
	# Plugin Updates
	- **mason-lspconfig.nvim**
	- **lazy.nvim**
	- **nvim-lspconfig**
	Press ENTER or type command to continue
	```
- [x] figure out a way to handle keymaps cleanly, as a test -> map `Esc` to `jk`
- [x] add a solution to search for files & text within a workspace, e.g. telescope
- [x] figure out a way to see warning/error, basically the wiggly lines in the editor, right now we can see the underline
- [x] make file/code aware UI. now, when i open neovim and then open a file, the coloring is same regardless of whether the file is python, txt, or a markdown. 
- [x] dotfiles are currently invisible when searching for files in telecope, we should add it.
- [x] when doing <fs>, i.e. search for a string, dotfiles aren't being inlcuded.
- [ ] integrate markdown preview feature into neovim (research the options)
- [ ] in a git repo, when i change a file i would like to see what lines of code have been edited, an indicator or a color change or some sort of a sign on the left line number column.
- [ ] add a solution to work with git easier in terminal. Current issue: whenever there's a merge conflict, i find it hard to resolve it in terminal - and refer to vscode. I would like to have an ability of moving around git seemlessly in command line.
